# Reflection

**Full document: [midcourse/reflection.md](midcourse/reflection.md)** — 469 words.

Covers the three modes the assistant was used in (reconnaissance, constrained
generation, execution); the reconnaissance catch that prevented a broken
`PATCH`; the Break Test script that left a file broken because it had no
`try/finally`; and the test review that found an assertion which could not fail.

> Written first person as the student’s own reflection — **read it and make the
> voice yours before submitting.**
